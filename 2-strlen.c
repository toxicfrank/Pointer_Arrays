#include "main.h"

/**
 * _strlen - function that returns the length of a string.
 * @s: pointer to a character
 * Return: count
 */

int _strlen(char *s)
{
	int count;

	count = strlen(s);

	return (count);
}
